{
  "name": "handcraftbandit-square-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Secure Square API proxy for irishbelthouse.com (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
